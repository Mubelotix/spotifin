[package]
name = "spotify-server"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = "0.5.1"
rocket_ws = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["fs", "io-util", "macros", "process", "rt-multi-thread", "time"] }
